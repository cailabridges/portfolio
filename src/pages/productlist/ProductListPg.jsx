import "./productListPg.css";
import Product from "../product/ProductPg";
import { products } from "../../data";

const ProductList = () => {
  return (
    <div className="pl">
      <div className="pl-texts">
        <h1 className="pl-title">Breaking barriers and codes, one line at a time </h1>
        <p className="pl-desc">
        Explore stunning designs, innovative projects, and much more.
        </p>
      </div>
      <div className="pl-list">
        {products.map((item) => (
          <Product key={item.id} img={item.img} link={item.link} />
        ))}
      </div>
    </div>
  );
};

export default ProductList;
